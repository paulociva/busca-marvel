import {ArrayFilterByValuePipe} from './array-filter-by-value.pipe';

describe('ArrayFilterByValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayFilterByValuePipe();
    expect(pipe).toBeTruthy();
  });
});
